apiVersion: v1
kind: Service
metadata:
  name: vault-injector-svc
  labels:
    app.kubernetes.io/name: vault-injector
spec:
  ports:
  - port: 443
    targetPort: 8080
  selector:
    app.kubernetes.io/name: vault-injector
    component: webhook
